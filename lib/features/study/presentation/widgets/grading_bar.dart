import 'package:flutter/material.dart';

class GradingBar extends StatelessWidget {
  const GradingBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
