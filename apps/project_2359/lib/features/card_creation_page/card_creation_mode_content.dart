import 'package:flutter/material.dart';

class CardCreationModeContent extends StatelessWidget {
  const CardCreationModeContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Card Creation");
  }
}
