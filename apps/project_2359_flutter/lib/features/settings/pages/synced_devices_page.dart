import 'package:flutter/material.dart';
import 'settings_boilerplate_page.dart';

class SyncedDevicesPage extends StatelessWidget {
  const SyncedDevicesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SettingsBoilerplatePage(title: 'Synced Devices');
  }
}
