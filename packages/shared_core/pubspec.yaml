name: shared_core
description: "Shared core logic for Project 2359 workspace"
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  pdfrx: ^2.2.24

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
